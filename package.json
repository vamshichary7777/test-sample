{
  "name": "lambda-hello-world",
  "version": "1.0.0",
  "description": "A simple Lambda function",
  "main": "index.js",
  "dependencies": {
    "image-size": "^0.10.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
